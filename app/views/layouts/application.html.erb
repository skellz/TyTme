<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>


  <body>
    <nav class="top-bar">
      <ul class="title-area">
        <li class="name"><h1><%= link_to 'TyTme', root_path %></h1></li>
        <li class="toggle-topbar"></li>
      </ul>
      <section class="top-bar-section">
        <ul class="right">
          <li><%= link_to 'Charities', charities_path %></li>
        </ul>

        <ul class="right">
          <li>
          <% if user_signed_in? %>
            <%= link_to 'User Account', edit_user_registration_path %>
          <% else %>
            <%= link_to 'User Sign In', user_session_path %>
          <% end %>
          </li>
        </ul>
      </section>
    </nav>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>